# 造词功能说明

## 功能概述

新增的造词功能允许用户通过选择单个汉字来创建自定义词组，系统会自动保存到用户词库并更新词频。

## 使用方法

1. **输入拼音**：输入两个或多个汉字的拼音（例如：`zhongguo` 或 `zhongguoren`）

2. **查看候选词**：
   - 系统会显示完整词组的候选（如"中国"、"中国人"）
   - 在候选词列表的末尾会显示第一个音节的所有单字选项（如"中"、"钟"、"终"、"种"等）
   - 可以通过翻页查看所有单字候选

3. **选择单字开始造词**：
   - 选择一个单字候选（例如选择"中"）
   - 系统进入造词模式，自动显示剩余音节的候选词

4. **灵活组合**：
   - 可以继续选择单字（如"国"），形成 1+1 组合
   - 也可以选择多字词（如"国人"），形成 1+2 组合
   - 支持任意组合方式：1+1、1+2、2+1、1+1+1 等

5. **自动保存**：
   - 当所有音节都选择完成后，系统会自动将组合的词组保存到用户词库
   - 词频初始值为1，每次使用该词时词频会自动增加
   - 下次输入相同拼音时，新创建的词组会出现在候选列表中，且随着使用频率提升排序会更靠前

## 实现细节

### 代码修改

1. **TypeDuckInputController.swift**
   - 添加了 `wordCreationCharacters` 和 `wordCreationPinyins` 数组来跟踪造词过程
   - 修改 `suggest()` 函数，在多音节拼音输入时自动添加所有单字候选（不限制数量）
   - 修改 `aftercareSelection()` 函数，改进造词逻辑：
     - 判断是否已经在造词过程中（`wordCreationCharacters.isNotEmpty`）
     - 如果选择单字且（已在造词中 OR 还有多个音节），则进入/继续造词模式
     - 完成所有选择后自动保存到 UserLexicon
   - 修改 `clearBufferText()` 函数，清空时重置造词状态

### 工作流程

1. 用户输入多音节拼音（≥2个音节）
2. 系统在候选列表末尾添加第一个音节的所有单字选项
3. 用户选择单字后进入造词模式：
   - 系统记录选择的字和拼音
   - 从 buffer 中移除已使用的拼音
   - 继续显示剩余拼音的候选
   - 在剩余拼音的候选列表末尾也添加单字选项
4. 在造词模式中，用户可以：
   - 继续选择单字（如"国"）
   - 或选择多字词（如"国人"）
   - 系统会将所有选择的文字组合起来
5. 当所有拼音都处理完成且选择了至少2个部分时：
   - 系统将组合的词组保存到 UserLexicon
   - 词频设置为1
6. 后续使用该词时：
   - UserLexicon 会自动增加词频（通过 `UserLexicon.handle()` 的 `update()` 方法）
   - 词频越高，该词在候选列表中的排序越靠前

### 词频更新机制

- 新造的词初始词频为1
- 每次选择该词时，`UserLexicon.handle()` 会：
  1. 查找该词是否已存在（通过 word + romanization 的 hash）
  2. 如果存在，调用 `update(id: id, frequency: frequency + 1)` 增加词频
  3. 如果不存在，插入新词条
- UserLexicon 查询时按 `frequency DESC` 排序，词频高的词排在前面

### 与现有功能的兼容性

- 不影响现有的词组输入功能
- 不影响单音节拼音输入
- 与用户词库功能完全兼容
- 造词过程可以随时通过 ESC 键取消
- 选择多字词组会退出造词模式，回到正常输入模式

## 示例

### 示例1：创建新词（1+1 组合）

输入：`zhongguo`

候选列表（第1页）：
1. 中国
2. 中果
3. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guo` 的候选：
1. 国
2. 果
3. 过
4. 郭
...

选择"果"后，系统自动保存"中果"到用户词库（词频=1）。

### 示例2：1+2 组合造词（徐两两）

输入：`xulianlian`

候选列表：
1. 徐连连
2. ...
9. 徐（单字）
10. 许（单字）

选择"徐"后，进入造词模式，显示 `lianlian` 的候选：
1. 连连
2. 两两
3. 恋恋
...

选择"两两"后，系统自动保存"徐两两"到用户词库（词频=1）。

### 示例3：1+2 组合造词（中国人）

输入：`zhongguoren`

候选列表：
1. 中国人
2. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guoren` 的候选：
1. 国人
2. 过人
3. ...

选择"国人"后，系统自动保存"中国人"到用户词库（词频=1）。

### 示例4：1+1+1 组合造词

输入：`zhongguoren`

候选列表：
1. 中国人
2. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guoren` 的候选：
1. 国人
2. ...
9. 国（单字）
10. 果（单字）

选择"国"后，继续造词模式，显示 `ren` 的候选：
1. 人
2. 任
3. 仁
...

选择"人"后，系统自动保存"中国人"到用户词库（词频=1）。

### 示例3：2+1 组合造词

输入：`zhongguoren`

候选列表：
1. 中国人
2. 中国
3. ...

选择"中国"后，**不会**进入造词模式（因为没有选择单字）。

如果想用 2+1 方式造词，需要先输入 `zhongguo`，选择"中国"，然后再输入 `ren` 选择"人"。
或者使用现有的输入记忆功能（`selectedCandidates`）。

### 示例5：使用已造词

再次输入：`zhongguo`

候选列表：
1. 中国
2. 中果（用户词库，词频=1）
3. ...

选择"中果"后，词频自动增加到2。

下次输入 `zhongguo` 时，"中果"的排序会更靠前（因为词频更高）。

## 修复的问题

### 问题1：单字显示不完整
- **原因**：使用了 `.prefix(10)` 限制只显示10个单字
- **修复**：移除了数量限制，现在会显示所有单字候选（最多100个，由 Engine 的 LIMIT 控制）
- **效果**：用户可以通过翻页查看所有单字选项

### 问题2：词频不更新
- **原因**：造词逻辑判断不够准确，导致某些情况下无法正确进入造词模式
- **修复**：改进了 `aftercareSelection()` 的逻辑：
  - 增加了 `isInWordCreation` 判断，检查是否已经在造词过程中
  - 条件改为：`isSingleCharSelected && (isInWordCreation || hasMultipleSyllables)`
  - 这样可以确保一旦开始造词，后续的单字选择都会继续造词流程
- **效果**：造词完成后会正确保存到 UserLexicon，词频会正常更新

### 问题3：1+2 组合方式不能记忆（如"徐两两"）- 模糊音问题
- **原因**：
  1. 原逻辑只允许单字选择继续造词，选择多字词会重置造词状态
  2. 计算已使用拼音长度时使用了不可靠的 `candidate.input.count`
  3. 对于多字词（如"两两"），`candidate.input` 可能不准确，导致 buffer 计算错误
  4. **关键问题**：UserLexicon 查询不支持模糊音
     - 用户输入 `xulianlian`（使用 an 音）
     - 数据库保存的是 `xu liang liang`（标准 ang 音）
     - 查询时 `lianlian` ≠ `liangliang`，无法匹配
- **修复**：
  1. 重构了造词逻辑，分为两个阶段：
     - **已在造词模式**：接受任何候选（单字或多字词），继续组合
     - **进入造词模式**：只有选择单字且还有多个音节时才进入
  2. 改进了拼音移除算法：
     - 根据 `candidate.romanization` 中的空格数计算音节数
     - 使用 `segmentation` 来精确计算应该移除的字符数
     - 例如："两两"的 romanization 是 "liang liang"（2个音节），从 segmentation 中取前2个 token 的长度
     - Fallback 到使用 `candidate.input` 并处理声调数字（与正常模式一致）
  3. **添加模糊音支持到 UserLexicon**：
     - 修改 `UserLexicon.suggest()` 方法
     - 在查询时使用 `FuzzyPinyinExpander.expandArray()` 生成模糊音变体
     - 例如：用户输入 `lianlian`，会同时查询 `liangliang`、`lianlian` 等变体
     - 使用 Set 去重，避免重复候选
- **效果**：
  - ✅ 支持 1+1、1+2、2+1、1+1+1 等任意组合方式
  - ✅ 一旦进入造词模式，可以灵活选择单字或多字词
  - ✅ 正确计算已使用的拼音长度，确保 buffer 处理准确
  - ✅ 支持模糊音查询，即使用户输入与标准拼音不同也能匹配
  - ✅ 例如：输入 `xulianlian`（an音），选择"徐"→"两两"（ang音），成功保存并能查询到"徐两两"

