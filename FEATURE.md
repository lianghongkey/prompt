# 造词功能说明

## 功能概述

新增的造词功能允许用户通过选择单个汉字来创建自定义词组，系统会自动保存到用户词库并更新词频。

## 使用方法

1. **输入拼音**：输入两个或多个汉字的拼音（例如：`zhongguo` 或 `zhongguoren`）

2. **查看候选词**：
   - 系统会显示完整词组的候选（如"中国"、"中国人"）
   - 在候选词列表的末尾会显示第一个音节的所有单字选项（如"中"、"钟"、"终"、"种"等）
   - 可以通过翻页查看所有单字候选

3. **选择单字开始造词**：
   - 选择一个单字候选（例如选择"中"）
   - 系统进入造词模式，自动显示剩余音节的候选词

4. **灵活组合**：
   - 可以继续选择单字（如"国"），形成 1+1 组合
   - 也可以选择多字词（如"国人"），形成 1+2 组合
   - 支持任意组合方式：1+1、1+2、2+1、1+1+1 等

5. **自动保存**：
   - 当所有音节都选择完成后，系统会自动将组合的词组保存到用户词库
   - 词频初始值为1，每次使用该词时词频会自动增加
   - 下次输入相同拼音时，新创建的词组会出现在候选列表中，且随着使用频率提升排序会更靠前

## 实现细节

### 代码修改

1. **PromptInputController.swift**
   - 添加了 `wordCreationCharacters` 和 `wordCreationPinyins` 数组来跟踪造词过程
   - 修改 `suggest()` 函数，在多音节拼音输入时自动添加所有单字候选（不限制数量）
   - 修改 `aftercareSelection()` 函数，改进造词逻辑：
     - 判断是否已经在造词过程中（`wordCreationCharacters.isNotEmpty`）
     - 如果选择单字且（已在造词中 OR 还有多个音节），则进入/继续造词模式
     - 完成所有选择后自动保存到 UserLexicon
   - 修改 `clearBufferText()` 函数，清空时重置造词状态

### 工作流程

1. 用户输入多音节拼音（≥2个音节）
2. 系统在候选列表末尾添加第一个音节的所有单字选项
3. 用户选择单字后进入造词模式：
   - 系统记录选择的字和拼音
   - 从 buffer 中移除已使用的拼音
   - 继续显示剩余拼音的候选
   - 在剩余拼音的候选列表末尾也添加单字选项
4. 在造词模式中，用户可以：
   - 继续选择单字（如"国"）
   - 或选择多字词（如"国人"）
   - 系统会将所有选择的文字组合起来
5. 当所有拼音都处理完成且选择了至少2个部分时：
   - 系统将组合的词组保存到 UserLexicon
   - 词频设置为1
6. 后续使用该词时：
   - UserLexicon 会自动增加词频（通过 `UserLexicon.handle()` 的 `update()` 方法）
   - 词频越高，该词在候选列表中的排序越靠前

### 词频更新机制

- 新词初始词频为 1000
- **所有选择都会记录**（包括首选候选词），使词频反映长期真实使用比例
- 每次选择某词时，`UserLexicon.handle()` 会：
  1. **衰减同音竞争词**：同 pinyin 的其他词频率 ×0.9（最低保底 1），防止同音词频率互相覆盖
  2. 查找该词是否已存在（通过 word + pingForm 的 hash）
  3. 如果存在，词频 +1000
  4. 如果不存在，插入新词条（初始频率 1000）
- UserLexicon 查询时按 `frequency DESC` 排序，词频高的词排在前面
- 当任意词频超过 1,000,000,000 时，全表词频减半（保持相对比例）

### 与现有功能的兼容性

- 不影响现有的词组输入功能
- 不影响单音节拼音输入
- 与用户词库功能完全兼容
- 造词过程可以随时通过 ESC 键取消
- 选择多字词组会退出造词模式，回到正常输入模式

## 示例

### 示例1：创建新词（1+1 组合）

输入：`zhongguo`

候选列表（第1页）：
1. 中国
2. 中果
3. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guo` 的候选：
1. 国
2. 果
3. 过
4. 郭
...

选择"果"后，系统自动保存"中果"到用户词库（词频=1）。

### 示例2：1+2 组合造词（徐两两）

输入：`xulianlian`

候选列表：
1. 徐连连
2. ...
9. 徐（单字）
10. 许（单字）

选择"徐"后，进入造词模式，显示 `lianlian` 的候选：
1. 连连
2. 两两
3. 恋恋
...

选择"两两"后，系统自动保存"徐两两"到用户词库（词频=1）。

### 示例3：1+2 组合造词（中国人）

输入：`zhongguoren`

候选列表：
1. 中国人
2. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guoren` 的候选：
1. 国人
2. 过人
3. ...

选择"国人"后，系统自动保存"中国人"到用户词库（词频=1）。

### 示例4：1+1+1 组合造词

输入：`zhongguoren`

候选列表：
1. 中国人
2. ...
9. 中（单字）
10. 钟（单字）

选择"中"后，进入造词模式，显示 `guoren` 的候选：
1. 国人
2. ...
9. 国（单字）
10. 果（单字）

选择"国"后，继续造词模式，显示 `ren` 的候选：
1. 人
2. 任
3. 仁
...

选择"人"后，系统自动保存"中国人"到用户词库（词频=1）。

### 示例3：2+1 组合造词

输入：`zhongguoren`

候选列表：
1. 中国人
2. 中国
3. ...

选择"中国"后，**不会**进入造词模式（因为没有选择单字）。

如果想用 2+1 方式造词，需要先输入 `zhongguo`，选择"中国"，然后再输入 `ren` 选择"人"。
或者使用现有的输入记忆功能（`selectedCandidates`）。

### 示例5：使用已造词

再次输入：`zhongguo`

候选列表：
1. 中国
2. 中果（用户词库，词频=1）
3. ...

选择"中果"后，词频自动增加到2。

下次输入 `zhongguo` 时，"中果"的排序会更靠前（因为词频更高）。

## 修复的问题

### 问题1：单字显示不完整
- **原因**：使用了 `.prefix(10)` 限制只显示10个单字
- **修复**：移除了数量限制，现在会显示所有单字候选（最多100个，由 Engine 的 LIMIT 控制）
- **效果**：用户可以通过翻页查看所有单字选项

### 问题2：词频不更新
- **原因**：造词逻辑判断不够准确，导致某些情况下无法正确进入造词模式
- **修复**：改进了 `aftercareSelection()` 的逻辑：
  - 增加了 `isInWordCreation` 判断，检查是否已经在造词过程中
  - 条件改为：`isSingleCharSelected && (isInWordCreation || hasMultipleSyllables)`
  - 这样可以确保一旦开始造词，后续的单字选择都会继续造词流程
- **效果**：造词完成后会正确保存到 UserLexicon，词频会正常更新

### 问题3：1+2 组合方式不能记忆（如"徐两两"）- 模糊音问题
- **原因**：
  1. 原逻辑只允许单字选择继续造词，选择多字词会重置造词状态
  2. 计算已使用拼音长度时使用了不可靠的 `candidate.input.count`
  3. 对于多字词（如"两两"），`candidate.input` 可能不准确，导致 buffer 计算错误
  4. **关键问题**：UserLexicon 查询不支持模糊音
     - 用户输入 `xulianlian`（使用 an 音）
     - 数据库保存的是 `xu liang liang`（标准 ang 音）
     - 查询时 `lianlian` ≠ `liangliang`，无法匹配
- **修复**：
  1. 重构了造词逻辑，分为两个阶段：
     - **已在造词模式**：接受任何候选（单字或多字词），继续组合
     - **进入造词模式**：只有选择单字且还有多个音节时才进入
  2. 改进了拼音移除算法：
     - 根据 `candidate.romanization` 中的空格数计算音节数
     - 使用 `segmentation` 来精确计算应该移除的字符数
     - 例如："两两"的 romanization 是 "liang liang"（2个音节），从 segmentation 中取前2个 token 的长度
     - Fallback 到使用 `candidate.input` 并处理声调数字（与正常模式一致）
  3. **添加模糊音支持到 UserLexicon**：
     - 修改 `UserLexicon.suggest()` 方法
     - 在查询时使用 `FuzzyPinyinExpander.expandArray()` 生成模糊音变体
     - 例如：用户输入 `lianlian`，会同时查询 `liangliang`、`lianlian` 等变体
     - 使用 Set 去重，避免重复候选
- **效果**：
  - ✅ 支持 1+1、1+2、2+1、1+1+1 等任意组合方式
  - ✅ 一旦进入造词模式，可以灵活选择单字或多字词
  - ✅ 正确计算已使用的拼音长度，确保 buffer 处理准确
  - ✅ 支持模糊音查询，即使用户输入与标准拼音不同也能匹配
  - ✅ 例如：输入 `xulianlian`（an音），选择"徐"→"两两"（ang音），成功保存并能查询到"徐两两"


#  新的排序规则（按优先级）

1. 用户词库优先级最高（order < 0）
   - 用户词库内部按频率排序（频率越高，order 值越小）
   - 不管是精确匹配还是模糊音匹配，用户词库都排在系统词库前面
2. 系统词库内部（order ≥ 0）
   - 精确匹配优先于模糊音匹配
   - 输入长度（更长的更具体）
   - 候选词长度（更短的更常用）
   - 数据库 rowid（更小的更常用）

这样的设计确保：
- 用户选择过的高频词（如连续两次选中的词）会始终排在第一位
- 在系统词库中，"bunen" 输入时，精确匹配 "bu nen" 的词会排在模糊匹配 "bu neng"（不能）的词前面
- 但如果用户曾经选择过"不能"，它会因为用户词库的高优先级而排在最前面

# 当输入只能拆分成 N 个音节时，不会匹配超过 N 个汉字的候选词

# 应该优先匹配相同音节数的候选词

#  直接英文状态切换

支持在英文状态输入一半的过程中直接切换为中文状态
前面已经输入的几个连续字母，直接作为拼音输入。再选择了指定的汉字之后，前面输入的字母替换为汉字。 
在英文模式下，字母应该看起来完全像普通文本（没有下划线或任何标记），
但实际上是未确认的 marked text，这样才能在Shift切换时被替换
- 英文模式的字母现在是 marked text（但看起来像普通文本）
- 切换到中文时直接替换 marked text（无需计算范围，更可靠）
- 所有中断操作（空格、标点、数字等）都会先提交文本
1. 切换到英文模式 
2. 输入 "nihao"（现在应该能看到字母显示） 
3. 按 Shift 切换到中文模式 
4. "nihao" 应该被替换为拼音输入 

# 交叉引用过滤（Cross-Reference Filter）

## 功能概述

在输入拼音后，通过按住 Shift 输入一个"辅助词"的拼音，系统找出辅助词中的字与目标词共同音节的交集，从而快速定位生僻字。解决了"知道某个字在某个词里，但单独按拼音很难翻到"的问题。

## 使用方法

1. **输入目标词拼音**：例如输入 `xiguan`，候选显示 习惯、吸管、西关...
2. **按住 Shift 输入辅助词拼音**：例如 Shift+`chengxi`
   - marked text 显示为：`xi guan CHENGXI`（大写表示过滤拼音）
   - 输入过程中，候选词显示为空（让用户知道正在过滤中）
3. **候选词变为单字列表**：当 filterText 形成有效共同音节后，系统找到共同音节 `xi`，从"城西/晨曦/承袭"等词中提取 xi 位置的字，显示为过滤后的单字候选
4. **选择目标字**：选择"曦"后进入造词模式，buffer 变为 `guan`，可继续用 Shift+filter 选择后续字

## 交互细节

| 按键 | 行为 |
|------|------|
| Shift+字母（buffering 时） | 追加到 filterText，实时过滤 |
| Backspace（过滤中） | 一次性清空整个 filterText，恢复原候选 |
| Escape（过滤中） | 清空 filterText，恢复原候选 |
| 空格/数字选择 | 选择候选词，同时清空 filterText |

## 过滤算法

```
输入:
  bufferText = "xiguan"    → bufferScheme = [xi, guan]
  filterText = "chengxi"   → filterScheme = [cheng, xi]

步骤:
1. 找第一个共同音节: xi（按 buffer 中的顺序）
2. 查辅助词: Engine.suggest("chengxi") → 城西, 承袭, 晨曦, 乘隙...
3. 提取 xi 位置的字: {西, 袭, 曦, 隙, 习, 熙, 溪, 喜...}
4. 查 xi 的所有单字候选，过滤保留交集中的字
5. 返回过滤后的单字候选列表（按 Engine 频率排序）
```

## 边界情况

| 情况 | 处理 |
|------|------|
| filterText 无完整音节（如 "ch"） | 候选显示为空 |
| 无共同音节（如 filter="dama", buffer="xiguan"） | 候选为空 |
| 未输入任何拼音/无候选词时 Shift+字母 | 不进入过滤，正常输入大写字母 |
| 造词模式中 Shift+字母 | 可以进入过滤，为后续字使用交叉引用 |
| 非 buffering 时 Shift+Space | 仍触发语音识别，不受影响 |

## 示例

### 示例1：输入"曦光"

输入 `xiguang`，候选：西光、希光、息光...（曦排很后面）

按 Shift 输入 `chenxi`：
- 共同音节：xi
- 从"晨曦/陈曦"提取 xi 位置的字：曦、曦
- 候选变为：西、系、细、戏、锡、喜、袭、希、溪、习、熙、曦...
- 选择"曦"→ 进入造词模式，buffer 变为 `guang`
- 选择"光"→ 输出"曦光"，保存到用户词库

### 示例2：输入"梁鸿"

输入 `lianghong`，候选：良好(?)、两行(?)...

按 Shift 输入 `lianghao`：
- 共同音节：liang
- 从"良好/梁浩"等提取 liang 位置的字：良、梁、亮、量...
- 候选变为：良、梁、亮、量、凉...
- 选择"梁"→ 进入造词模式，buffer 变为 `hong`